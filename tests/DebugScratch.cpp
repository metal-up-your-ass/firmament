// Intentionally empty. This translation unit exists only because the local
// sandbox this session ran in does not permit removing files once created
// (no `rm`/`mv`); it briefly held an ad hoc diagnostic TEST_CASE used to
// isolate a test bug (see the "gain staging" note in GainStagingTests.cpp
// and the "why test 3 was rewritten" note in the handoff summary). Its
// actual replacement/permanent content lives in GainStagingTests.cpp.
